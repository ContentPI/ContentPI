import { styled } from '@styles/theme'

export const StyledContent = styled.div`
  .model {
    position: relative;
    display: flex;
    justify-content: space-between;
  }

  .rows {
    min-height: 80vh;
    margin-top: 20px;
  }
`

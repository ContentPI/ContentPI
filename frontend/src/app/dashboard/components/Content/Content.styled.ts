import { styled } from '@styles/theme'

export const StyledContent = styled.div`
  margin: 0 auto;
  width: 98%;

  .model {
    position: relative;
    display: flex;
    justify-content: space-between;
  }
`
